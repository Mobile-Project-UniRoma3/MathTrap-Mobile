<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:res="clr-namespace:MathTrap"
             mc:Ignorable="d"
             x:Class="MathTrap.HelpPage">
    <ContentPage.Content>
        <StackLayout BackgroundColor="#FFB200">
            <Grid RowSpacing="2">
                <Label Text="" x:Name="label31" VerticalOptions="Center" HorizontalOptions="Center" TextColor="Black" FontSize="28" ></Label>
            </Grid>
            <Grid  VerticalOptions="Center" HorizontalOptions="Center" >
                <ScrollView>
                    <Label x:Name="textcell1" VerticalOptions="Center" HorizontalOptions="Center"  TextColor="Black" FontSize="16" ></Label>
                </ScrollView>
            </Grid>
            <Grid>
                <Button Text="{x:Static res:MathTrapResources.help_Button1}" Clicked="onMain" TextColor="Black" x:Name="help_Button1"  BackgroundColor="AliceBlue" ></Button>
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>